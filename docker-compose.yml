services:
  ner_rag_bot:
    build: .
    environment:
      - OPENAI_API_KEY={"OPENAI_API_KEY"}
      - TELEGRAM_BOT_TOKEN={"TELEGRAM_BOT_TOKEN"}
      - PYTHONPATH=/app
    volumes:
      - .:/app
    restart: always
